# Main Swarm Router Requirements
# ================================
# These packages should already be available in your project environment

# Core dependencies (from main project)
numpy>=1.26.0
joblib>=1.3.0
shap>=0.44.0

# API dependencies (from AI model requirements)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0

# ML dependencies (from AI model requirements)
scikit-learn>=1.4.0
xgboost>=2.0.0

# LLM Integration (Groq)
groq>=0.4.0
